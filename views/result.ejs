<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <%- include('_menu', { page: 'results' }) %>

        <h2>Pathfinder Results</h2>
        <pre><%= output %></pre>

        <% if (showImages) { %>
            <div class="floors">
                <div>
                    <h3>Floor 1 Path</h3>
                    <img src="/floor1_path.png" width="800">
                </div>
                <div>
                    <h3>Floor 2 Path</h3>
                    <img src="/floor2_path.png" width="800">
                </div>
            </div>
            <% } %>

                <a href="/interior">← Try another</a>
</body>

</html>